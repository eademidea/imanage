
describe("Test UserController...", () => {
    it("Test ok when call method signup", async () => {
        // const { body, status } = await global.testRequest.get('/v1/user/signin');
        // expect(status).toBe(200);

        console.log("test")
    })
})